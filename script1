pipeline {
    agent { label 'master' }
    stages {
        stage('ALL') {
            parallel {
                stage('build') { 
                    steps {
                         sh''' 
                            echo "build stage"
                         '''
                 }
             }
                stage('deploy') {
                    steps {
                         sh'''
                          echo "deploy stage"
                         '''
                  }
             }
         }
     }
   }     
}
